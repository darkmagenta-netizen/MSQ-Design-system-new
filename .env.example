# Figma REST API token (required for private files).
# Create at: Figma → Settings → Account → Personal access tokens
# Used by /api/figma-asset for crypto logos from MSQ Design System – Working.
FIGMA_ACCESS_TOKEN=

# GitHub repo for fork count in home navbar (e.g. "owner/repo" or "msq/design-system").
# Navbar link and badge use this; leave empty to hide fork count and link to github.com.
NEXT_PUBLIC_GITHUB_REPO=
